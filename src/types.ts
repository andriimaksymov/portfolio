export type Experience = {
  company: string;
  role: string;
  period: string;
  location: string;
  highlights: string[];
};

export type SkillGroup = {
  title: string;
  skills: string[];
};

export type Project = {
  name: string;
  link: string;
  subtitle: string;
  stack: string[];
  description: string;
  impact: string[];
};

export type ValueCard = {
  title: string;
  description: string;
};

